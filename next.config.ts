import type { NextConfig } from "next";

// Served at the root of interview-rounds.nextjoblist.com (a subdomain), so no
// basePath is needed.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
